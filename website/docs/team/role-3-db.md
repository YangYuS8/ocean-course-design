# 分工 3：数据库与基础模型

## 负责范围

- `inspection_tasks` 表。
- `samples` 表。
- `users` 表基础字段。
- `InspectionTask` 和 `Sample` 模型基础关系。
- `DatabaseSeeder` 初始数据。

## 必背内容

```text
一个巡检任务可以有多个样本，一个样本属于一个巡检任务。
```

## 遇到复杂追问

如果老师问到异常自动生成、登录中间件、完整业务流程，可以回答：

> 我主要负责数据库表和基础模型关系，这个问题由负责后端总体架构或核心业务控制器的同学补充。
